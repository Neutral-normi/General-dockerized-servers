# General-dockerized-servers
Template dockerized server with
- Frontend React
- Backend Express
- Database PostgreSQL
- Docker Compose

To initiate:

1. Clone the repository
2. Create the environment file, don't forget to change user and password
3. Run:

```bash
docker compose up --build
